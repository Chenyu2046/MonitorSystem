syntax = "proto3";
package monitor.proto;

// Worker 主机级 load average；Linux /proc/loadavg 的窗口实际为 1/5/15 分钟。
// load_avg_3 保留历史字段名，但实际对应 5 分钟窗口；它不等同于逐核 CPU busy 百分比。
message CpuLoad {
    float load_avg_1 = 1;
    float load_avg_3 = 2;
    float load_avg_15 = 3;
  }
