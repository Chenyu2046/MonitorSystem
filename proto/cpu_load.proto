syntax = "proto3";
package monitor.proto;

// Worker 主机级 load average；1/3/15 分钟窗口用于调度压力观察，
// 不等同于逐核 CPU busy 百分比。
message CpuLoad {
    float load_avg_1 = 1;
    float load_avg_3 = 2;
    float load_avg_15 = 3;
  }
